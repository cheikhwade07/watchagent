"""Data access layer.

Will provide insert-with-dedup helpers (for readings and events) plus query
functions used by the /readings and /events endpoints and the /health counts.

Phase 2 implements this.
"""

# Phase 2: implement insert-with-dedup and query functions.
