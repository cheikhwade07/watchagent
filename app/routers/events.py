"""Events endpoint.

Will expose GET /events to return detected weather events (e.g. temperature
spikes, sustained rain) produced by the detection pipeline.

Phase 4 implements this against the storage layer.
"""

# Phase 4: define router and GET /events backed by repository queries.
