# Copy this file to .env and adjust as needed. Never commit a real .env.

# Filesystem path to the SQLite database file. In Docker this points into the
# mounted /data volume so the database persists across container restarts.
WATCHAGENT_DB_PATH=/data/watchagent.db
