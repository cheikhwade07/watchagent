"""Shared pytest fixtures.

Will hold shared fixtures such as a TestClient bound to the app, a temporary
SQLite database, and sample readings/events for later phases.
"""

# Phase 2+: add shared fixtures (test client, temp DB, sample data).
