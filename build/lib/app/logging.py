"""Structured logging setup.

Future home for configuring structured (JSON) logging for the API, poller,
and detection pipeline so logs are machine-parseable in production.

Phase 2 implements this.
"""

# Phase 2: provide a configure_logging() helper invoked at startup.
