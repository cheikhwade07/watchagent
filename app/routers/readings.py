"""Readings endpoint.

Will expose GET /readings to return stored weather readings (with filtering
by location and time range).

Phase 2 implements this against the storage layer.
"""

# Phase 2: define router and GET /readings backed by repository queries.
