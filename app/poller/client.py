"""Open-Meteo HTTP client.

Async client (httpx-based) for fetching current weather readings from the
Open-Meteo API for the configured locations.

Phase 3 implements this; httpx will be added as a runtime dependency then.
"""

# Phase 3: implement an async fetch function using httpx.AsyncClient.
